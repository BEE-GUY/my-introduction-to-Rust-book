fn main() {
    println!("Hello, world!");
}

// -> cargo build 
// self explanatory.

// -> cargo check
// checks if code will compile without creating a binary.

// -> cargo run
// same as "cargo build" but runs the code too.

// -> --release
// adding this flag will add all the optimizations, but it will compile longer.

